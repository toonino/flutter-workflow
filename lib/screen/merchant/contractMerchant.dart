import 'package:flutter/material.dart';

class ContractMerchant extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hợp đồng'),
    );
  }
}
